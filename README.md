"# Vedio-Confrencing-WebApp" 
A web application for video meeting + realtime text chat. Developed with Node.js, Express, Peer.js and Socket.io.

Usage
npm install
npm run dev

npm i -g peer
peerjs --port 3001

Go to localhost:3000
